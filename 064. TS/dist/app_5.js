// Напишите программу, которая находит сумму всех чисел от 1 до 100 и выводит
// результат в консоль.
const maxNum = 100;
let sum = 0;
for (let i = 0; i < maxNum; i++) {
    sum += i;
}
console.log(sum);
